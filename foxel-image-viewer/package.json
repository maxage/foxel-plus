{
  "name": "foxel-image-viewer-plus",
  "version": "1.1.3",
  "description": "A powerful image viewer plugin for Foxel with advanced features",
  "main": "dist/plugin.js",
  "scripts": {
    "build": "node -e \"const pkg = require('./package.json'); const fs = require('fs'); let content = fs.readFileSync('src/index.tsx', 'utf8'); content = content.replace(/const VERSION = '[^']*';/, \\`const VERSION = '\\${pkg.version}';\\`); content = content.replace(/author: '[^']*'/, \\`author: '\\${pkg.author}'\\`); fs.writeFileSync('src/index.tsx', content);\" && esbuild src/index.tsx --bundle --format=iife --platform=browser --target=es2019 --jsx=automatic --minify --outfile=dist/plugin.js --define:process.env.NODE_ENV='\"production\"'",
    "dev": "esbuild src/index.tsx --bundle --format=iife --platform=browser --target=es2019 --jsx=automatic --outfile=dist/plugin.js --watch",
    "clean": "rm -rf dist",
    "validate": "node validate-plugin.js",
    "prebuild": "npm run validate"
  },
  "keywords": ["foxel", "plugin", "image", "viewer"],
  "author": "Jason",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^18.2.79",
    "@types/react-dom": "^18.2.25",
    "esbuild": "^0.25.9",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
